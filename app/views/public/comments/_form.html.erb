<%= form_with(model: [review,comment]) do |f| %>
  <div class="form-group">
    <%= f.text_area :comment_text, rows: '1', placeholder: "コメント(最大50文字)", class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit "送信", class: "btn btn-primary" %>
  </div>
<% end %>